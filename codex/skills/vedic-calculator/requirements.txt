# vedic-calculator 依赖清单
#
# ⚠️ 请使用 scripts/setup_env.py 自动安装。
# 原因: PyJHora 未声明其运行时依赖（numpy/geocoder/geopy/requests/timezonefinder/python-dateutil），
# 且 pip 包缺少 .se1 星历文件；setup_env.py 会补齐依赖并从 pysweph 复制星历。
#
# 手动安装顺序（如果不用 setup_env.py）：
#   pip install pysweph>=2.10.3.5 pytz>=2024.1 numpy geocoder geopy requests timezonefinder python-dateutil
#   pip install PyJHora==4.8.6
#
# Python 版本要求: 3.8 ~ 3.13（pysweph 为 C 扩展，暂无 3.14 预编译包）
#
pysweph>=2.10.3.5
PyJHora==4.8.6
pytz>=2024.1
